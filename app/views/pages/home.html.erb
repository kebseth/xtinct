<div class="banner banner-home" style="background-image: linear-gradient(rgba(0,0,0,0.0),rgba(0,0,0,0.0)), url(https://source.unsplash.com/user/fridooh);">
  <div class="container col-md-4 mr-auto">
    <h1>Xtinct,<strong> rent your favorite animal</strong>!</h1>
    <p>Change your life and rent an animal of your choice around the world.</p>

    <%= form_tag animals_path, method: :get do %>
      <div class="form-row">
        <div class="form-group col-md-12">
          <%= text_field_tag :query,  params[:query], class: "form-control", placeholder: "American Lion, Nantes" %>
        </div>
      </div>
      <!-- <div class="form-row">
        <div class="text-black form-group col-md-6">
          <label for="inputdate">From</label>
          <input type="date" class="form-control" id="inputdate" placeholder="From">
        </div>
        <div class="text-black form-group col-md-6">
          <label for="inputdate">To</label>
          <input type="date" class="form-control" id="inputdate" placeholder="To">
        </div>
      </div>
      <button type="submit" class="btn btn-success b-rounded">Rent an animal</button>
    </form>
      </div> -->
      <%= submit_tag "Rent an animal", class: "btn btn-primary" %>
    <% end %>
  </div>
</div>


<div class="container">
  <div class="row mt-3">
    <div class="col-xs_2-lg-10">
      <h2 class="animal-listing-title text-center m-4">OUR LAST ANIMAL ADDED</h2>
      <div class="cards-listing-animals col lg-6">
        <% @animals.each do |animal| %>
          <%= link_to animals_path(animal), class: "text-decoration-none" do  %>
            <div class="card-category-animals mb-3">
              <%= image_tag cl_image_path(animal.photo, height: 600, width: 700, crop: :fill), alt: "alttext" %>
              <div class="card-infos-animals">
                <div>
                  <h2><%= animal.name %></h2>
                  <p><%= animal.species %></p>
                </div>
                <h2 class="card-animals-pricing"><%= animal.price_per_day %> € / day.</h2>
                <%= image_tag cl_image_path(animal.owner.avatar), class: "avatar-bordered", alt: "avatar-bordered" %>
              </div>
            </div>
          <% end  %>
        <% end %>
      </div>
    </div>
  </div>
</div>
